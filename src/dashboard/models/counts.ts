export class Counts {
  userCount: number;
  postCount: number;
  commentCount: number;

  // Initialize all with zero
  constructor() {
    this.userCount = 0;
    this.postCount = 0;
    this.commentCount = 0;
  }
}
